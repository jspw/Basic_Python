#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<double>v;
    int n;
    while(cin>>n){
        v.push_back(sqrt(n))
    }
    for(int i=v.size()-1;i>=0;i++)
    cout<<v[i]<<endl;
    return 0;
}